import heroImage from '../images/rick.jpg';

const Hero = () => {
    return (
        <section className='hero'>
            <img src={heroImage} alt="Rock Band" />
        </section>
    )
}

export default Hero
